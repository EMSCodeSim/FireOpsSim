# State pathway overlays

Put optional state-specific pathway overrides here, for example:

- `firefighter-i.json` — only the fields that differ from the national pack
- reciprocity notes
- extra required modules unique to this state

Prefer thin overlays. Do not duplicate the full national JPR list unless the state structure is truly different.
